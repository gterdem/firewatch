"""Decision-store adapter package (ADR-0072 D2/D8) — cases-adapter pattern.

``schema.py`` owns the DDL; ``sqlite_decisions.py`` is the concrete
``DecisionStore`` (``firewatch_core.ports.decisions``) implementation.
"""
