"""Core-side adapters — concrete implementations of the SDK ports.

Each adapter lives in its own module and must be imported explicitly; this
package intentionally exports nothing so callers choose exactly what they need.
"""
