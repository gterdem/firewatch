"""firewatch_core.ports — protocol definitions for core-owned ports.

Each module defines a Protocol (port) that adapters must satisfy.
These are imported by core internals only — never by plugins.
"""
